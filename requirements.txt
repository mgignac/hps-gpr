# HPS GPR Analysis Dependencies
# Install with: pip install -r requirements.txt

# Core scientific computing
numpy>=1.20
pandas>=1.3
scipy>=1.7

# Machine learning
scikit-learn>=1.0

# Plotting
matplotlib>=3.4

# ROOT file reading
uproot>=4.0

# CLI and configuration
click>=8.0
pyyaml>=6.0

# Note: The local 'gp' package must be installed separately
# (typically via pip install -e /path/to/gp-repo)
